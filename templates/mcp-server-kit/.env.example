# Port for the streamable-HTTP transport (only used with --http).
PORT=3000

# The example tools call no external APIs. If you add a tool that needs a
# secret, read it from the environment here — never hardcode it in source.
# WEATHER_API_KEY=...
