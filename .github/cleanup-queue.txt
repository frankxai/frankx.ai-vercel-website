# Branch cleanup queue.
# Replace the entries below in a pull request; do not append a new batch to stale entries.
# The Branch Cleanup workflow deletes exactly the non-comment lines after merge.
# Clear a completed queue through a follow-up pull request because main is PR-protected.
# See BRANCH_AUDIT.md for the canonical safe-delete list.

# --- Queue is empty. ---
#
# The last batch (16 archive/* duplicates) merged as #425 and the workflow
# deleted all 16. Each of those refs was re-checked against the live remote
# immediately before this commit: all 16 are gone.
#
# Clearing an executed batch is step 4 of the workflow header above, and it had
# not been done — so this file still read as a pending 16-branch deletion while
# actually being a no-op. An empty queue is the correct resting state.
#
# The next batch replaces these comment lines with branch names. It does not
# append below them.
