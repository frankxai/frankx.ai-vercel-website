# Sentinel daily brief — 2026-05-07

**Portfolio:** 6 sites monitored
- 🔴 3 red · 🟡 3 yellow · 🟢 0 green

## Top actions

1. **Arcanea** — `/manifesto` returns 404
1. **Arcanea** — `/realms` returns 404
1. **Arcanea** — `/seekers` returns 404
1. **VibeClubs** — `/clubs` returns 404
1. **VibeClubs** — `/sessions` returns 404
1. **FrankX** — `/os` returns 404

## Sites

### 🔴 Arcanea · `arcanea.ai`

- **Crawl** 🔴 1/4 routes 200 — broken:
  - `/manifesto` → 404
  - `/realms` → 404
  - `/seekers` → 404

### 🔴 Starlight Intelligence · `starlight-intelligence.ai`

- **Crawl** 🔴 0/4 routes 200 — broken:
  - `/` → fetch failed
  - `/protocol` → fetch failed
  - `/layers` → fetch failed
  - `/canon` → fetch failed
- **Schema** 🔴 4/4 routes have invalid JSON-LD

### 🔴 VibeClubs · `vibeclubs.ai`

- **Crawl** 🔴 1/3 routes 200 — broken:
  - `/clubs` → 404
  - `/sessions` → 404

### 🟡 FrankX · `frankx.ai`

- **Crawl** 🟡 7/10 routes 200 — broken:
  - `/os` → 404
  - `/start-here` → 404
  - `/build` → 404
- **Sitemap** 🟡 6 critical routes not in sitemap.xml:
  - `/os`
  - `/library`
  - `/design`
  - `/network`
  - `/start-here`
- **Robots** 🟡 0 AI bots blocked, 16 partial — verify intent
- **DNS** 🟢 DNS query failed (resolver issue), but site is HTTP-reachable — treating as healthy
- **Open Graph** 🟡 4 OG-image duplicates across routes
- **Internal links** 🟡 5 orphan routes (no inbound links from configured set)
  - `/os`
  - `/design`
  - `/network`
- **Freshness** 🟡 1 stale routes:
  - `/research` · 82d old · rule `/` (max 60d)

### 🟡 GenCreator · `gencreator.ai`

- **Crawl** 🟡 2/4 routes 200 — broken:
  - `/manifesto` → 404
  - `/playbook` → 404

### 🟡 Arcanea App · `arcanea-ai-appx.vercel.app`

- **Crawl** 🟡 2/3 routes 200 — broken:
  - `/console` → 404
- **Schema** 🟡 1/3 routes have invalid JSON-LD

---

_Generated 2026-05-07T07:27:03.890Z by [Sentinel](https://github.com/frankxai/sentinel)._