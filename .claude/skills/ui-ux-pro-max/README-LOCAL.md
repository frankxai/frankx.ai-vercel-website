# Local notes — ui-ux-pro-max

This skill is **vendored** from [nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)
via `frankxai/claude-skills-library` → `packs/web-excellence`. Do not hand-edit
`SKILL.md`, `data/`, `references/`, or `scripts/` — re-running the pack's
`install.sh` replaces them (the previous copy is set aside under
`.claude/skills/.replaced/`, which is gitignored).

**`data/prompts.csv` is local-only** — 24 style-prompt rows that do not exist
upstream, carried over from the older fork that used to live here. The upstream
query scripts do not read it. Kept because the content is useful on its own; if
you want it queryable, it needs re-adding to the upstream data loader or moving
somewhere the scripts see.
