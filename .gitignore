# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/
!/app/build/
!/app/library/build/
dist/

# Design token exports — regenerate with `npm run design:tokens`
.design-tokens/
.design-tokens.tailwind.json
.design-tokens.dtcg.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Misc
.cache/
/public/reading/
/reading-site/

# Local agent scratch files
notes.md
task_plan.md
content/**/CLAUDE.md
components/**/CLAUDE.md
data/**/CLAUDE.md
lib/**/CLAUDE.md
FrankX.AI - Vercel Website/
# Agentic Creator OS - ignore MCP package, track core files
agentic-creator-os/*
!agentic-creator-os/ACOS-V4-SPEC.md
!agentic-creator-os/ACOS-V5-SPEC.md
!agentic-creator-os/CLAUDE.md
!agentic-creator-os/ARCHITECTURE.md
!agentic-creator-os/hub-generator/
.claude-skills/CLAUDE.md
# AI Skills templates (not for production)
Claude-Code-AI-Architect-Skills/
tina-frankx/

# Local artifacts (multi-agent + tooling)
.worktrees/
.playwright-mcp/
.claude-plugins-fcakyon/
_archive/
repo_sync_report.md
research/production-llm-agents-2026/
C:\\Users\\Frank\\.claude\\plugins\\marketplaces\\claude-plugins-official/

# Local runtime artifacts
.claude-flow/daemon.pid
.claude-flow/daemon-state.json
.claude-flow/learning/
.claude-flow/audit.jsonl
.claude-flow/metrics/
.claude-flow/swarm/
.claude-flow/governance/
nul

# ACOS session artifacts (regenerated per-session, never commit)
.acos/
.claude/checkpoints/
.claude/trajectories/
.claude/worktrees/

# Nested production-repo clones — MUST NOT be committed to the FrankX dev repo
# Production deploys from sibling at C:/Users/frank/frankx.ai-vercel-website/
# See memory: feedback_audit_nested_repos.md + docs/ops/HANDOVER-2026-04-20-*.md
frankx.ai-vercel-website/
frankxai/

# Additional nested sibling repos that landed inside the working tree.
# Each has its own .git/ — they are separate projects that should live as
# siblings under C:/Users/frank/, not nested. Frank to relocate when convenient.
frankx/
frankx-os/
/ai-architect-academy/
education/context-engineering-for-creators/
claude-skills/
products/mcp-doctor/
products/notion-powered-blog/
products/storage-intelligence/
products/suno-mcp-server/
products/visual-intelligence/

# Local-only book project hub at root (separate from content/books/, app/books/, etc.)
# Leading slash prevents matching content/books/, app/books/, public/images/books/.
/books/

# Runtime state — regenerated, never commit
.pp/
ruvector.db
.claude/scheduled_tasks.lock
.gh-api-body.json
.frankx/machine/agent-harness-audit.json
.frankx/machine/github-harness-inventory.json

# Local-only Claude Code settings (per-developer environment allow lists)
.claude/settings.local.json

# Weekly automated reports (often INCOMPLETE due to source-files-not-mounted)
# Kept on disk for inspection but never committed
weekly-financial-health-*.md

content/hooks/inbox/*.csv

# ACOS AgentDB — vector store + ReasoningBank, regenerated locally
# Never commit: contains embeddings + run history that's per-developer state
data/acos/agentdb/*.db
data/acos/agentdb/*.db-*
data/acos/agentdb/cache/

# Private partner briefs — named contacts, transition timing, contact angles.
# Public partner pages live in app/partnerships/ and content/partnerships/.
# Anything under .frankx/private/ is operator-only and never reaches the site.
.frankx/private/

# Operator-only substrates — NEVER commit to this PUBLIC repo.
# These live in the private FrankX dev repo. Tracking them here would
# leak machine inventory, family biographical files, and credential paths.
# Background: agy attempted to sync them on 2026-05-30; caught pre-push.
.hearth/
.umwelt/
.frankx/machine/

# Orphan drag-drops that don't belong at repo root — assets go under public/
/connect-live.png
/connect-qr.png

# 404 telemetry + agentic backfill queue (route-recovery system, Phases 2+3).
# Operator surface at /admin/404-radar reads these; committing them would leak
# request URLs and personal browsing patterns. The directory is created lazily
# by /api/404/log on first hit.
private/

# Python bytecode (skill helper scripts)
__pycache__/
*.pyc

# AIS-emit outputs (generated by scripts/sync-ais.mjs, never committed in FrankX)
/public/llms.txt
/public/llms-full.txt
/public/agents.json
/public/schema-graph.json
/public/sitemap-extras.xml

# Starlight/ACOS temporary files
.agent/
.system_generated/
.next-backup/
temp-*.json
link-checker-report.json
link-checker-report.txt

# Starlight/ACOS temporary files
.starlight/
.gemini/

# Playwright e2e test artifacts (regenerated on every run)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
