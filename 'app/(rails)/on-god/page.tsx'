import type { Metadata } from 'next';
import { HubPage, buildHubMetadata } from '@/lib/rails/render-helpers';

export const metadata: Metadata = buildHubMetadata('god');

export default function OnGodPage() {
  return <HubPage hub="god" />;
}
