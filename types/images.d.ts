/// <reference types="next/image-types/global" />

// CI type-checks before Next generates the ignored next-env.d.ts. Keep static
// image imports typed on a fresh checkout as well as during the production build.
