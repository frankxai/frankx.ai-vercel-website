# Runbook — personal AI centre of excellence

Fixture: the exit statuses below are this worked example's stated values. On your machine,
`gate.operate` is green only after you have run each command yourself and recorded the status you
observed.

**This runbook does not pass its gate.** Step 5r names a binary that does not resolve on this
machine, so the rollback for the memory store has never been dry-run. That is recorded in the
table, not smoothed over: an untested restore is the difference between a backup and a habit.

| step | command | dry-run exit | rollback | owner |
|---|---|---|---|---|
| 1. check tool presence | `command -v node rg` | 0 | none needed — read only | operator |
| 2. check the model seam | `rg -l "from ['\"](openai\|@anthropic-ai/sdk\|@google/genai)" -tsrc` | 0 (1 file) | none needed — read only | operator |
| 3. register the scheduled tasks | `pwsh -File scripts/schedule/register-tasks.ps1 -WhatIf` | 0 | step 3r | operator |
| 3r. unregister the scheduled tasks | `pwsh -File scripts/schedule/unregister-tasks.ps1 -WhatIf` | 0 | — | operator |
| 4. run one loop by hand | `node scripts/run-loop.mjs brief --dry-run` | 0 | none needed — writes to a scratch directory | operator |
| 5. migrate the memory store | `node scripts/memory-migrate.mjs --dry-run` | 0 (would move 1,412 entries) | step 5r, **which does not run** | operator |
| 5r. restore the memory store from backup | `restic restore latest --target ~/.coe/memory` | **not run — `command -v restic` exits 1, binary absent** | — | operator |
| 5b. restore from the plain archive instead | `node scripts/memory-import.mjs --from ~/.coe/archives/latest.tar.gz --dry-run` | **not run — no archive exists yet; `scripts/memory-export.mjs` has never been scheduled** | — | operator |
| 6. measure the kill criterion | `node scripts/brief-open-rate.mjs --last 30` | 0 | none needed — read only | operator |
| 7. run the eval suite | `node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl` | 0 (13 pass) | none needed — read only | operator |
| 8. stop everything | `pwsh -File scripts/schedule/unregister-tasks.ps1` | 0 | re-run step 3 | operator |

## Procedures

### Daily operation

Nothing. Three scheduled tasks run and write files. The operator reads them. If the machine is
asleep, the run is skipped and not caught up (`02-user-flows.md`, flow 1 failure branch).

### Memory store migration — blocked

```bash
node scripts/memory-migrate.mjs --dry-run    # exit 0, "would move 1,412 entries"
```

**Do not run this without `--dry-run` yet.** Its rollback is step 5r, and step 5r calls a binary
that is not installed:

```bash
command -v restic                            # exit 1, no output
```

Owner: the operator. Two ways to unblock, and either one closes `gate.operate`:

1. Install the snapshot backup tool the backup script already writes for, then run
   `restic restore latest --target /tmp/restore-test` against a scratch target and record the exit
   status here. Fixture estimate: 40 minutes.
2. Or drop the dependency: schedule `node scripts/memory-export.mjs` nightly so that step 5b has an
   archive to restore from, dry-run `scripts/memory-import.mjs`, and record its exit status.
   Fixture estimate: 90 minutes, and it removes one external tool from the system.

Until one of the two is done and its exit status is written into the table above, the migration
does not run and `gate.operate` stays `FAIL`.

### Nightly backup

```bash
node scripts/backup.mjs
```

Owner: the operator. Rollback: step 5r or 5b, neither of which currently works — see above. The
backup itself succeeds; the honest description of the current state is "one-way copy", not "backup".

### Weekly eval run

```bash
node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl
```

Owner: the operator. Rollback: not applicable — read only.

### Turn everything off

```bash
pwsh -File scripts/schedule/unregister-tasks.ps1
```

Owner: the operator. Rollback: re-run step 3. Nothing is lost: the notes, the memory store and the
briefs are files on disk and outlive the loops that wrote them.

## Alerts

None. There is no pager, no queue depth, and no second person to notify. The signal is a missing
brief in the morning, which the operator notices because the kill criterion is about whether they
open it at all.

Generated by AI Architect · https://www.frankx.ai/ai-architect
