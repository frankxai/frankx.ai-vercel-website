# Verification receipt — 2026-08-23

Written by `independent-verifier` in a fresh context. The verifier read the artifacts and the
machine, and read no other agent's reasoning. It re-derives; it does not repair.

## Limit of this receipt

This artifact set is a **fixture** shipped with the AI Architect plugin. The machine it describes
is the example's definition, so every pointer below was re-derived against that definition and not
against a live system. A pointer confirmed against a fixture is evidence about the fixture and
nothing else. Recorded as finding F3 in `review.md`.

A second limit, specific to this example: in a one-person system the verifier is also the operator
wearing a third hat. The fresh context is real — no other agent's reasoning was read — but the
independence is procedural, not organisational, and nothing in this receipt should be read as
though a second person checked the work.

## Evidence pointers re-derived

| id | pointer or command | recorded observation | re-observed | verdict |
|---|---|---|---|---|
| `ev.model.seam` | `rg -l "from ['\"](openai\|@anthropic-ai/sdk\|@google/genai)" -tsrc` | 1 file | 1 file, `packages/model/src/route.ts` | CONFIRMED |
| `ev.loop.exit` | `rg -n "budget.remaining" packages/loops/src/run.ts` | 1 hit, L58 | 1 hit, L58 | CONFIRMED |
| `ev.trust.label` | `rg -n "untrusted_web" packages/prompt/src/context.ts` | 1 hit, L33 | 1 hit, L33 | CONFIRMED |
| `ev.trust.callers` | `rg -l "buildContext\(" -tsrc` | 3 files | 3 files, all in `packages/loops/` | CONFIRMED |
| `ev.run.home` | `rg -n "Register-ScheduledTask" scripts/schedule/register-tasks.ps1` | 1 hit, L27 | 1 hit, L27 | CONFIRMED |
| `ev.tools.send` | `rg -n "send_mail\|publish_post" packages/tools/src` | 0 hits | 0 hits | CONFIRMED |
| `ev.operate.restore` | `command -v restic` | exit 1, no output | exit 1, no output | CONFIRMED — the absence is real, which is why `gate.operate` is red |
| `ev.evals.count` | `wc -l docs/architecture/06-evals/cases.jsonl` | 13 | 13 | CONFIRMED |

8 pointers, 8 CONFIRMED, 0 FAILED. Note that `ev.operate.restore` being CONFIRMED is a confirmation
that something is missing. A gate can be red with every pointer holding; the pointers are how the
red is proved, not a score.

## Fenced commands re-run

| command | recorded | re-observed |
|---|---|---|
| `command -v node rg` | exit 0 | exit 0 |
| `command -v restic` | exit 1 | exit 1 |
| `node scripts/memory-migrate.mjs --dry-run` | exit 0, "would move 1,412 entries" | exit 0, same line |
| `node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl` | 13 cases, 13 pass, 0 fail | 13 cases, 13 pass, 0 fail |
| `node scripts/brief-open-rate.mjs --last 30` | exit 0 | exit 0 |
| `node scripts/check-roi.mjs <this directory>` — its literal return-rate-acronym scan | 0 hits | 0 hits |

`scripts/memory-import.mjs --from ~/.coe/archives/latest.tar.gz --dry-run` was **not** re-run: the
archive it names does not exist, so there is nothing to dry-run against. The runbook says so in the
same words rather than recording a hopeful exit 0.

## Arithmetic re-derived

`04-roi.md` claims $15.88 a month. Re-derived from `prices.json` and the `fx.*` assumptions:
2.25M × $1.00 = $2.25; 0.225M × $5.00 = $1.125; 2.4M × $2.00 = $4.80; 0.32M × $10.00 = $3.20;
0.6M × $5.00 = $3.00; 0.048M × $25.00 = $1.20; 20 GB × $0.015 = $0.30. Sum = $15.875, stated as
$15.88. CONFIRMED.

Payback base case re-derived: 17.5 h × $60 = $1,050; less $15.88 = $1,034.12; $1,800 ÷ $1,034.12 =
1.74 months. CONFIRMED. The verifier notes that `04-roi.md` states the cancellation of the hourly
rate out of the payback ratio, and re-derived it: at $30 an hour, benefit $525 − $15.88 = $509.12
and build $900, giving 1.77 months — the same answer to within the run-cost rounding, as claimed.

## Gate statuses

| gate | status | note |
|---|---|---|
| `gate.frame` | PASS | two kill criteria, each with a threshold, a date and a measurement |
| `gate.discovery` | PASS | zero `[assumed]`; two `[unknown]`, both owned by the operator |
| `gate.flow` | PASS | 3 flows, 3 failure-branch sections, 3 human-step sections, all stages in the blueprint |
| `gate.decisions` | PASS | four decisions MADE with file:line evidence; every plane owned, by the same person, recorded as such |
| `gate.economics` | PASS | 7 sourced rows, arithmetic re-derived above, no return-rate acronym |
| `gate.trust` | PASS | label line present; both irreversible writes gated; `run_backup` recorded as finding F1 |
| `gate.evals` | PASS | 13 cases, 3 refusal, 3 injection, all failing closed |
| `gate.operate` | **FAIL** | `command -v restic` exits 1, so the memory-store rollback has never been dry-run, and the fallback path has no archive to restore from. Two named fixes and an owner are in `review.md`. The verifier re-ran the check and confirms the gate is red |
| `gate.verify` | PASS | this receipt |

## What the verifier wrote

`docs/architecture/receipts/2026-08-23-verify.md` and `docs/architecture/review.md`. Nothing else.

Generated by AI Architect · https://www.frankx.ai/ai-architect
