# Review — personal AI centre of excellence

What is unowned or untested, first. No score is assigned.

## Untested, and therefore not owned

**F1 — the restore path has never run, and `gate.operate` is red because of it.**
The nightly job writes an encrypted archive to object storage and exits 0 every night, which reads
like a working backup. The other half is missing: `restic restore` is the rollback for the
memory-store migration, and `command -v restic` exits 1 on this machine. The fallback route is also
dead — step 5b restores from a plain archive that `scripts/memory-export.mjs` would produce, and
that script has never been scheduled, so there is no archive.

The memory store is the only artifact in this system that is expensive to lose. Everything else is
regenerable: briefs, drafts, run logs, the index. So the honest description of the current state is
a one-way copy, not a backup, and the migration in `07-runbook.md` step 5 must not run until this
is closed.

Named fix, either half closes the gate. Owner: the operator, who is the only candidate.

1. Install the snapshot backup tool the backup script already writes for, restore to a scratch
   target, and record the exit status in the runbook table. Fixture estimate: 40 minutes.
2. Or remove the dependency: schedule `node scripts/memory-export.mjs` nightly, dry-run
   `scripts/memory-import.mjs`, and record the exit status. Fixture estimate: 90 minutes, and the
   system ends up with one fewer external tool.

Option 2 costs more and leaves the system smaller, which is usually the better trade in a
one-person system. Either way, `gate.operate` stays `FAIL` until an exit status is written down.

**F2 — every human gate is held by the person who requests it.**
Three gated actions, all approved by the operator, usually seconds after the operator asked for
them. This is inherent to a system with one human in it and cannot be engineered away. What is
engineered: only three tools are gated, and the promote step caps candidates at seven a night, so
that acceptance stays an act rather than a reflex. Recorded as a standing limit rather than a
finding to close.

**F3 — this artifact set is a fixture.** Every path, command and observed output is the example's
own definition. The verifier's eight CONFIRMED pointers are internally consistent and say nothing
about anyone's real machine. Re-derive before reusing.

## Open, with an owner and a date

| id | open item | owner | by when |
|---|---|---|---|
| `g1` | restore untested, backup tool absent | the operator | before the memory-store migration; this is F1 |
| `g2` | other agent harnesses on this machine have unaudited read access under `~/.coe/` | the operator | before any credential is stored there |
| `g3` | the 45-minute daily baseline is self-reported from two weeks of timer entries, and it is the load-bearing input of `04-roi.md` | the operator | at the kill-criterion measurement, 2026-09-22 |

## Escalations

- 2026-08-23 — `gate.operate` red at the `operate` stage. The missing input is an exit status that
  only the operator can produce, on their own machine, after installing a tool or scheduling an
  export. Escalated with the gate id, the two candidate fixes and their commands. No agent in this
  workflow can close it, and none tried.

## What is fine, briefly

One model-call seam with routing by task, which is where most of this system's cost discipline
lives. Labelled context with a different tag per tier, including a tag on the operator's own notes.
Memory that only a person can write to. A kill criterion that measures whether the operator opens
the brief, which is a better test of this system than any rubric.

Also worth saying: at $15.88 a month against a stated $25 line, the economics of this system are
not about tokens at all. `04-roi.md` shows a 30% swing in token volume (baseline = the fixture
token counts) moving payback by about half a day. If the operator ever wants to make this cheaper,
the lever is the weekly ledger's half-hour, not the prompt.

## What would change this review

An exit status written next to step 5r or 5b. That is the whole list.

Generated by AI Architect · https://www.frankx.ai/ai-architect
