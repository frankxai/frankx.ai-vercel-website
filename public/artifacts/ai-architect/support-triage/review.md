# Review — support triage

What is unowned, first. No score is assigned, here or anywhere else in this artifact set.

## Unowned

**F1 — the model plane has no owner, and `gate.decisions` is red because of it.**
Three modules import a provider SDK directly (`packages/llm/src/client.ts`,
`apps/worker/src/summarize.ts`, `apps/api/src/routes/reply-draft.ts`;
`rg -l "from ['\"](openai\|@anthropic-ai/sdk)" -tsrc` → 3 files). Two feature teams each own
their own call site. Nobody owns the fallback path, the cache key, or the one line that would
show what this system spends. The `model` decision is `OPEN` with a dated deferral cost, which
the gate permits — the red is the unowned plane, not the open decision.

Named fix, in two parts because they have different owners:

1. The head of engineering names an owner for the model plane. This is a five-minute decision
   that has been open since discovery (gap `g3`, `01-discovery.md` § 44).
2. That owner collapses `apps/worker/src/summarize.ts` and `apps/api/src/routes/reply-draft.ts`
   into `packages/llm/src/client.ts`, then re-runs the grep and expects 1 file. Fixture estimate:
   12 engineer hours, which is not in `fx.build` and therefore not in `04-roi.md`'s payback.

Until both land, `gate.decisions` stays `FAIL`. It is not close to passing and should not be
described as passing with a caveat.

**F2 — `promote_index_namespace` is irreversible and has no human gate.**
Severity medium. The nightly index swap promotes a new namespace with no approval step. The
mitigations are real — the previous namespace is retained 24 hours and the rollback is one command
(`07-runbook.md`) — and the blast radius is search quality, not customer contact. The platform
engineer owns accepting this. What is not acceptable is leaving it undocumented, which is why it
is a row in `05-trust-boundary.md` rather than an omission.

**F3 — this artifact set is a fixture, and confirming a fixture confirms nothing about a real
system.** Every path, command and observed output in this example is the example's own
definition. The verifier's seven CONFIRMED pointers are internally consistent and externally
worthless. Anyone reusing this shape has to re-derive every pointer against their own repository
before a single line here counts as evidence.

## Open, with an owner and a date

| id | open item | owner | by when |
|---|---|---|---|
| `g1` | no first-reply-time series, so the benefit half of `04-roi.md` rests on a fixture assumption rather than a measurement | support lead | next cost review |
| `g2` | model provider's sub-processor status under the data processing agreement is unchecked | head of engineering | before any production traffic |
| `g3` | no run-level trace ties a model call, a tool call and a ticket together | platform engineer | with the F1 fix, or the observability plane's evidence column stays thin |
| `g4` | no budget figure; cost per ticket ($0.0197) is standing in for one | head of engineering | next cost review |

## Escalations

- 2026-08-23 — `gate.decisions` red at the `decide` stage. Escalated to the operator with the gate
  id, the missing input (an owner for the model plane) and the command that would produce the
  evidence. The operator chose to continue the run with the finding recorded rather than block
  stages 5 to 9. That choice is the operator's to make; recording it is not optional.

## What is fine, briefly

The trust boundary is real and traced to a line, the loop's exit condition is in code, the long
run is out of the request, and the system holds no send capability at all — the strongest control
in the design is a tool that does not exist. Twelve eval cases run and pass, including three
injection cases that fail closed.

None of that offsets F1. A system with an unowned model plane is one provider incident away from
discovering who owns it.

## What would change this review

An owner named for the model plane and one file returned by the seam grep. Everything else here
is either accepted with a name against it or scheduled with a date.

Generated by AI Architect · https://www.frankx.ai/ai-architect
