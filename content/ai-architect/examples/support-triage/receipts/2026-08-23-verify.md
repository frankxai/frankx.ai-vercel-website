# Verification receipt — 2026-08-23

Written by `independent-verifier` in a fresh context. The verifier read the artifacts and the
repository, and read no other agent's reasoning. It re-derives; it does not repair.

## Limit of this receipt

This artifact set is a **fixture** shipped with the AI Architect plugin. The repository it
describes is the example's definition, so every pointer below was re-derived against that
definition and not against a live system. A pointer confirmed against a fixture is evidence about
the fixture and nothing else. This limit is recorded as finding F3 in `review.md` rather than
hidden behind the word "confirmed".

## Evidence pointers re-derived

| id | pointer or command | recorded observation | re-observed | verdict |
|---|---|---|---|---|
| `ev.model.seam` | `rg -l "from ['\"](openai\|@anthropic-ai/sdk)" -tsrc` | 3 files | 3 files: `packages/llm/src/client.ts`, `apps/worker/src/summarize.ts`, `apps/api/src/routes/reply-draft.ts` | CONFIRMED |
| `ev.loop.exit` | `rg -n "attempts >= 3" apps/worker/src/triage.ts` | 1 hit, L64 | 1 hit, L64 | CONFIRMED |
| `ev.trust.label` | `rg -n "untrusted_ticket" packages/prompt/src/build.ts` | 1 hit, L27 | 1 hit, L27 | CONFIRMED |
| `ev.trust.callers` | `rg -l "buildPrompt\(" -tsrc` | 2 files | 2 files, both in `apps/worker` | CONFIRMED |
| `ev.run.ceiling` | `rg -n "enqueue\(" apps/api/src/routes/intake.ts` | 1 hit, L22 | 1 hit, L22 | CONFIRMED |
| `ev.tools.send` | `rg -n "send_email" apps/worker/src/tools.ts` | 0 hits | 0 hits | CONFIRMED |
| `ev.evals.count` | `wc -l docs/architecture/06-evals/cases.jsonl` | 12 | 12 | CONFIRMED |

7 pointers, 7 CONFIRMED, 0 FAILED.

## Fenced commands re-run

| command | recorded | re-observed |
|---|---|---|
| `command -v node rg jq` | exit 0 | exit 0 |
| `node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl` | 12 cases, 12 pass, 0 fail | 12 cases, 12 pass, 0 fail |
| `node scripts/index-macros.mjs --plan` | exit 0, "would embed 41 of 512 macros" | exit 0, same line |
| `node scripts/check-roi.mjs <this directory>` — its literal return-rate-acronym scan | 0 hits | 0 hits |
| `node -e "const p=require('./docs/architecture/prices.json');const cut=Date.now()-90*864e5;console.log('stale',p.rows.filter(r=>Date.parse(r.retrieved_at)<cut).length)"` | stale 0 | stale 0 |

## Arithmetic re-derived

`04-roi.md` claims a monthly run cost of $98.47. Re-derived from `prices.json` and the `fx.*`
assumptions in `00-frame.md`: 30M input tokens × $2.00 = $60.00; 3.5M output tokens × $10.00 =
$35.00; 0.015M reads × $16.00 = $0.24; 0.06M writes × $4.00 = $0.24; 2 GB × $0.33 = $0.66;
50,000 GB-s × $0.0000166667 = $0.83; 60 × $0.025 = $1.50. Sum = $98.47. CONFIRMED.

Payback base case re-derived: 208.33 h × $38 = $7,916.67; less $98.47 = $7,818.20; $11,900 ÷
$7,818.20 = 1.52 months. CONFIRMED. The verifier notes that both hourly rates are fixture
assumptions and not sourced prices, which `04-roi.md` states plainly — recorded, not scored.

## Gate statuses

| gate | status | note |
|---|---|---|
| `gate.frame` | PASS | kill criterion carries a threshold, a date and a measurement command |
| `gate.discovery` | PASS | zero `[assumed]`; four `[unknown]`, each with a named owner |
| `gate.flow` | PASS | 3 flows, 3 failure-branch sections, 3 human-step sections, every stage in the blueprint |
| `gate.decisions` | **FAIL** | `planes.model.owner` is `unowned`. Named fix and its owner are in `architecture.json` and `review.md`. The verifier re-derived the cause and confirms the gate is red, not borderline |
| `gate.economics` | PASS | 8 sourced rows, arithmetic re-derived above, no return-rate acronym |
| `gate.trust` | PASS | label line present, tool table complete, one irreversible tool recorded as a finding |
| `gate.evals` | PASS | 12 cases, 3 refusal, 3 injection, all failing closed |
| `gate.operate` | PASS | every command dry-run with an exit status; the one command that cannot be dry-run carries a read-only probe |
| `gate.verify` | PASS | this receipt |

## What the verifier wrote

`docs/architecture/receipts/2026-08-23-verify.md` and `docs/architecture/review.md`. Nothing else.
The verifier holds no editing tools and wrote both files by shell redirection.

Generated by AI Architect · https://www.frankx.ai/ai-architect
