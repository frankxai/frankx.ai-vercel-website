# Verification receipt — 2026-08-23

Written by `independent-verifier` in a fresh context. The verifier read the artifacts and the
repository, and read no other agent's reasoning. It re-derives; it does not repair.

## Limit of this receipt

This artifact set is a **fixture** shipped with the AI Architect plugin. The repository it
describes is the example's definition, so every pointer below was re-derived against that
definition and not against a live system. A pointer confirmed against a fixture is evidence about
the fixture and nothing else. Recorded as finding F3 in `review.md`.

## Evidence pointers re-derived

| id | pointer or command | recorded observation | re-observed | verdict |
|---|---|---|---|---|
| `ev.model.seam` | `rg -l "import (openai\|anthropic)" -tpy` | 1 file | 1 file, `services/qa/src/model.py` | CONFIRMED |
| `ev.loop.exit` | `rg -n "MAX_STEPS" services/qa/src/pipeline.py` | 1 hit, L72 | 1 hit, L72 | CONFIRMED |
| `ev.trust.label` | `rg -n "system_prompt \+=" services/qa/src/prompt.py` | 1 hit, L57 | 1 hit, L57 | CONFIRMED — the finding is real |
| `ev.trust.wrapper` | `rg -n "untrusted_clause" services/qa/src` | 0 hits | 0 hits | CONFIRMED — the label line is ABSENT |
| `ev.trust.callers` | `rg -l "build_prompt\(" -tpy` | 2 files | 2 files, both calling `services/qa/src/prompt.py` | CONFIRMED |
| `ev.run.home` | `rg -n "workflow.step" services/ingest/src/workflow.py` | 1 hit, L19 | 1 hit, L19 | CONFIRMED |
| `ev.context.asof` | `rg -n "effective_date <=" services/qa/src/retrieve.py` | 1 hit, L48 | 1 hit, L48 | CONFIRMED |
| `ev.evals.result` | `node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl` | 13 cases, 12 pass, 1 fail, exit 1 | 13 cases, 12 pass, 1 fail (`injection-002`), exit 1 | CONFIRMED |

8 pointers, 8 CONFIRMED, 0 FAILED.

Every pointer holds and two gates are red. That combination is the normal case, not a contradiction:
pointers are how a finding is proved, and a gate is a judgement about what the pointers show.

## Fenced commands re-run

| command | recorded | re-observed |
|---|---|---|
| `command -v node python rg` | exit 0 | exit 0 |
| `python -m services.ingest.reindex --plan` | exit 0, "would embed 412 of 12,000 clauses" | exit 0, same line |
| `node scripts/feature-toggle.mjs --list` | exit 0, `external_upload=off` | exit 0, `external_upload=off` |
| `node scripts/run-evals.mjs docs/architecture/06-evals/cases.jsonl` | exit 1, 12 pass, 1 fail | exit 1, 12 pass, 1 fail |
| `node scripts/citation-audit.mjs --sample 40 --since 2026-08-30` | exit 0 | exit 0 |
| `node scripts/check-roi.mjs <this directory>` — its literal return-rate-acronym scan | 0 hits | 0 hits |

## Arithmetic re-derived

`04-roi.md` claims $51.80 a month. Re-derived from `prices.json` and the `fx.*` assumptions:
16.2M × $2.00 = $32.40; 0.81M × $10.00 = $8.10; 5.4M × $1.00 = $5.40; 0.36M × $5.00 = $1.80;
0.0072M × $16.00 = $0.115 (stated $0.12); 0.012M × $4.00 = $0.048 (stated $0.05); 6 GB × $0.33 =
$1.98; 27,000 GB-s × $0.0000166667 = $0.45; 900 invocations = $0.0002; 40 GB × $0.015 = $0.60;
36 × $0.025 = $0.90. Unrounded sum = $51.79 against the stated $51.80; the cent is the two vector
lines, which `04-roi.md` rounds up to $0.12 and $0.05 before adding. CONFIRMED with that note — the
difference does not move any figure in the file, and the file rounds in the conservative direction.

Payback base case re-derived: 225 h × $95 = $21,375; less $51.80 = $21,323.20; 284 h × $110 =
$31,240; $31,240 ÷ $21,323.20 = 1.47 months, stated as 1.5. CONFIRMED.

The verifier notes that `04-roi.md` puts the trust-boundary remediation inside the build line rather
than in a footnote. That is the honest placement — the fix is not optional — and it moves base
payback from 1.34 months (260 hours) to 1.47.

## Gate statuses

| gate | status | note |
|---|---|---|
| `gate.frame` | PASS | two kill criteria, each with a threshold, a date and a measurement command |
| `gate.discovery` | PASS | zero `[assumed]`; two `[unknown]`, each with a named owner |
| `gate.flow` | PASS | 3 flows, 3 failure-branch sections, 3 human-step sections; the injection branch describes the current failure rather than an intended behaviour |
| `gate.decisions` | PASS | three MADE with ADRs, one OPEN with a dated deferral cost, seven planes owned |
| `gate.economics` | PASS | 11 sourced rows, arithmetic re-derived above, no return-rate acronym |
| `gate.trust` | **FAIL** | clause text is T3 and enters the instruction position at `services/qa/src/prompt.py:L57`; the label line is ABSENT. Named fix and owner in `review.md` |
| `gate.evals` | **FAIL** | `injection-002` fails open. Same cause as `gate.trust`; one fix closes both |
| `gate.operate` | PASS | every command dry-run with its exit status, including the honest exit 1 on the eval step; rollback and owner named per procedure |
| `gate.verify` | PASS | this receipt |

Fix-first order across the two red gates: they are one fix, and it is the `trust` decision, which is
first in the reversal-cost order anyway.

## What the verifier wrote

`docs/architecture/receipts/2026-08-23-verify.md` and `docs/architecture/review.md`. Nothing else.
The verifier holds no editing tools and wrote both files by shell redirection.

Generated by AI Architect · https://www.frankx.ai/ai-architect
