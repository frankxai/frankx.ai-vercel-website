---
title: "Do Not Campaign a Lie"
description: "Demand work is held until the public surface is honest. Matching a costume number to another page is still a lie if the number is unsourced. Hold the campaign. Fix the surface. Then talk."
date: "2026-08-17"
author: "FrankX"
category: "Intelligence Dispatches"
tags:
  - integrity
  - production-discipline
  - content-strategy
  - truth-first
  - quality-gates
image: "/images/blog/blog-hero-aurora.svg"
featured: false
keywords:
  - campaign integrity
  - content honesty
  - production discipline
  - truth-first content
  - quality standards
readingGoal: "Understand why demand work must be held until the public surface is honest, and why matching costume numbers without sources is still a lie."
tldr: "Demand work is held until the public surface is honest. Matching a costume number to another page is still a lie if the number is unsourced. Costume counters that look accurate but have no source are rejected. Hold the campaign. Fix the surface. Then talk."
---

**TL;DR**: Do not campaign a lie. If the public surface shows placeholder counters that no longer match reality, do not campaign that page—fix it first. If you replace placeholders with costume numbers that have no source, that is still a lie. Demand work (marketing, outreach, campaigns) must be held until the surface is honest. Fix the substrate, verify the claims, then talk.

---

## The Costume Number Problem

A costume number is a number that looks real but has no source. It matches the format of other numbers on the page. It sits in the same visual hierarchy. It might even be technically accurate—but if you cannot trace it back to a counted, auditable source, it is a lie dressed as a fact.

Here's a real example: A site showed placeholder counters (`0K` for content, `0` for repositories) that were accurate when first written but never updated as work accumulated. The surface claimed nothing existed when the actual inventory had grown substantially.

The obvious fix: replace the zeros with the current numbers.

The correct fix: replace the zeros with real numbers *derived from a source you can audit*.

The difference is integrity. If you hard-code numbers to match the visual spec, you have moved the problem, not solved it. Six months from now, the inventory will have grown again, and the hard-coded numbers will still be frozen. The number became a costume the moment it detached from its source.

A costume number is a lie, even when it happens to be accurate at the moment you publish it.

---

## Why You Cannot Campaign a Lie

Demand work is anything that asks others to pay attention: marketing, outreach, social campaigns, newsletter announcements, PR. Demand work assumes the surface you are pointing people toward is honest.

If the surface is dishonest, demand work compounds the problem. Every person who visits sees the lie. Some percentage will notice. A smaller percentage will call you on it. But 100% will form an opinion about your standards, whether they say so or not.

Here's the pattern I learned the hard way: **if the surface is not honest, hold all demand work.**

The failure mode looks like this:

1. You build a product or publish content.
2. The homepage stats are stale or invented.
3. You launch a campaign to drive traffic because "the product is ready."
4. Visitors arrive, see the dishonest surface, and leave.
5. The campaign succeeds at driving traffic and fails at building trust.

The correct sequence:

1. Audit the public surface. List every claim, number, and assertion.
2. Source every number. If it cannot be sourced, remove it or derive it from code.
3. Verify coherence: do the numbers on the homepage match the numbers in the docs, the blog, the dashboards?
4. Fix the lies before you launch the campaign.
5. *Then* talk.

This is slower. It is also the only version that compounds trust instead of eroding it.

---

## First-Hand Signal: The Arcanea Ticker Rejection

This estate hit this failure mode exactly. The Arcanea site (arcanea.ai) showed `0K+ words of canon` and `0 public repos` in its hero ticker. Those zeros were accurate when first written but never updated as the world and codebase grew.

The temptation was: replace the zeros with hard-coded numbers that match current reality. A proposed fix offered specific counts to replace the placeholders. That proposal was rejected—not because the numbers were wrong, but because they had no source.

The rejection happened for two reasons:

**It creates a maintenance burden.** Hard-coded numbers go stale the day after you publish them. Months later, you are showing a stat that no longer matches reality. The stat became a costume.

**It bypasses the actual problem.** The real failure was not "the zeros are wrong"—it was "there is no source of truth for these stats." Replacing placeholders with costume numbers does not fix that. It moves the dishonesty one layer deeper.

The correct fix:

1. Write a script that counts from the source of truth (files on disk, API responses, database queries).
2. Store the results in a version-controlled data file.
3. Import those numbers into UI components from the data file.
4. Run the script in CI so the numbers update automatically on every deploy.

Until that fix lands, Arcanea demand work stays held. Do not campaign arcanea.ai. Do not point readers to a surface that still shows placeholder zeros or unsourced replacements.

---

## Matching Costume Numbers Is Still a Lie

Here's a subtler failure mode: you fix one page to show sourced numbers, but you leave other pages with hard-coded "matching" numbers.

Example: The homepage shows a sourced word count (derived from a script). The About page repeats that same number in prose (hard-coded).

Those numbers match today. Six months from now, the homepage will auto-update, and the About page will still show the old number. You created coherence debt.

The rule: **if the number matters, it must be sourced or derived. If it is decoration, remove it.**

For secondary pages, the correct fix is one of three options:

1. Import the same data file and interpolate the number into the prose.
2. Replace the specific number with a durable claim that never goes stale (e.g., "extensively" instead of a count).
3. Link to the canonical source instead of claiming a number (the reader can verify if they care).

All three are honest. Hard-coding a number that will drift is not.

---

## When Honesty Blocks Velocity

The honest approach is slower. Sourcing numbers takes time. Writing auditable scripts takes time. Verifying coherence across every page takes time.

The fast approach is: hard-code the numbers, ship the campaign, and fix it later "if anyone notices."

The fast approach trades velocity for trust. It works exactly once—the first time, when you have no reputation to lose. Every subsequent lie costs more, because every person who trusted you before now has a reason to doubt.

Here's the decision tree:

**If the surface is honest:** Ship the campaign. The worst case is you grow faster than your infrastructure can handle. That is a good problem.

**If the surface is dishonest but the lie is obvious:** Hold the campaign. Fix the surface. Then ship. Example: a homepage that shows placeholder counters when the actual inventory is substantial and visible elsewhere on the site.

**If the surface is dishonest but the lie is subtle:** Still hold. Subtle lies are worse than obvious ones because readers assume you did not notice. Example: a homepage that shows one sourced stat next to an unsourced costume number.

The cost of holding is opportunity cost—you could have shipped today, you ship next week. The cost of lying is trust debt—you ship today, and some percentage of readers mark you as unreliable.

For a creator building a long-term brand, trust debt is the higher cost. Hold the campaign. Fix the surface. Then talk.

---

## What to Do This Week

Three actions. Pick one. Finish it.

1. **Audit your public-facing numbers.** Homepage stats, About page claims, case study counts, testimonials. List every number. Ask: where is the source? If you cannot point to a script, API response, or countable artifact, the number is a costume. Remove it or source it.

2. **Run the coherence audit.** Compare your homepage to your About page to your blog to your docs. Do the same claims appear in multiple places? Do the numbers match? If no, fix the divergence. If yes but they are hard-coded, derive them from a shared source.

3. **Build the auto-update script.** Pick one important stat (word count, repo count, case studies). Write a script that counts it from the source of truth. Store the result in JSON. Import the JSON into every page that uses the stat. Run the script in CI. Now that number updates automatically and never goes stale.

---

## FAQ

### What is a costume number?

A costume number is a number that looks real but has no source. It matches the format of other numbers on the page and sits in the same visual hierarchy, but if you cannot trace it back to a counted, auditable source, it is a lie dressed as a fact.

### Why is matching costume numbers a problem?

Even if the costume number is accurate today, it will go stale. Six months from now, the sourced numbers will auto-update and the costume numbers will stay frozen. You create coherence debt: some pages are honest, others drift, and readers cannot tell which is which.

### What counts as "demand work"?

Demand work is anything that asks others to pay attention: marketing, outreach, social campaigns, newsletter announcements, PR. It assumes the surface you are pointing people toward is honest. If the surface is dishonest, demand work compounds the problem by bringing more eyes to the lie.

### How do you make numbers auditable?

Write a script that counts from the source of truth: count words in published posts, query the GitHub API for repo counts, query your email provider for subscriber counts. Store results in JSON. Import JSON into UI components. Run the script in CI so numbers auto-update on deploy.

### What if fixing the surface delays your campaign?

That is the correct trade-off. The cost of holding is opportunity cost (you ship next week instead of today). The cost of lying is trust debt (readers mark you as unreliable). For a creator building a long-term brand, trust debt is the higher cost. Hold the campaign. Fix the surface. Then talk.

### How does this relate to reader-first content and the Golden Age?

This integrity discipline applies to the [Reader-First Golden Age](/blog/reader-first-golden-age) principle of putting the reader's time and trust first. It connects to [Coherence Is an Engineering Property](/blog/coherence-is-an-engineering-property) as a structural implementation of staying honest. The [Sovereign Curator](/blog/the-sovereign-curator) operates under this constraint: curate what is real, not what looks good.

---

## Related Reading

- [Reader-First Golden Age](/blog/reader-first-golden-age) — Putting the reader's time and trust first
- [Coherence Is an Engineering Property](/blog/coherence-is-an-engineering-property) — Structural integrity across your creative system
- [The Sovereign Curator](/blog/the-sovereign-curator) — Curating what is real, not what looks good
- [Get Started](/start) — See the integrity gates in practice

---

Do not campaign a lie. Demand work is held until the public surface is honest. Fix the substrate, verify the claims, then talk.
